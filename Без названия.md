## 📊 Технологии по категориям

```dataviewjs
try {
    const technologies = dv.pages('"Technologies"').filter(p => p.technology);
    
    if (technologies.length === 0) {
        dv.paragraph("❌ Файлы технологий не найдены.");
        return;
    }

    // Группируем по категориям
    const grouped = technologies.groupBy(p => p.category);
    
    let tableData = [];
    
    // Проходим по каждой категории
    for (const group of grouped) {
        // Добавляем заголовок категории
        tableData.push([
            `**${group.key}**`, 
            "", "", "", "", ""
        ]);
        
        // Добавляем технологии этой категории
        for (const p of group.rows) {
            const tasks = p.file.tasks || [];
            const totalTasks = tasks.length;
            const completedTasks = tasks.filter(t => t.completed).length;
            
            let status = "📝 В планах";
            let progress = "0%";
            
            if (totalTasks > 0) {
                const progressPercent = Math.round((completedTasks / totalTasks) * 100);
                progress = progressPercent + "%";
                status = completedTasks === totalTasks ? "✅ Изучено" : "🚧 В процессе";
            }
            
            const level = p.level && p.max_level ? `${p.level}/${p.max_level}` : "-";
            const updated = p.updated ? dv.date(p.updated).toLocaleDateString('ru-RU') : "-";
            
            tableData.push([
                "", // отступ для технологии
                p.file.link,
                status,
                level,
                progress,
                completedTasks + "/" + totalTasks,
                updated
            ]);
        }
        
        // Добавляем пустую строку между категориями
        tableData.push(["", "", "", "", "", "", ""]);
    }
    
    dv.table(
        ["Категория", "Технология", "Статус", "Уровень", "Прогресс", "Задачи", "Обновлено"],
        tableData
    );
    
} catch (error) {
    dv.paragraph("❌ Ошибка: " + error.message);
}
```