
# Объектно-Ориентированное Программирование в Java для Новичков

## Содержание ^soderganie-dlya-novichkov
- [Что такое ООП? Простыми словами](#^chto-takoe-oop-prosto)
- [Зачем нужно ООП?](#^zachem-nujno-oop)
- [Четыре кита ООП](#^chetyre-kita-oop)
  - [Инкапсуляция - "Сейф с данными"](#^inkapsulyaciya-dlya-novichkov)
  - [Наследование - "Семейное сходство"](#^nasledovanie-dlya-novichkov)
  - [Полиморфизм - "Одно имя, разные действия"](#^polimorfizm-dlya-novichkov)
  - [Абстракция - "Упрощение сложного"](#^abstrakciya-dlya-novichkov)
- [Первый класс - создаем свой объект](#^pervyj-klass-prosto)
- [Практические примеры из жизни](#^primery-iz-zhizni)
- [Частые ошибки новичков](#^oshibki-novichkov)
- [Что учить дальше?](#^chto-uchit-dalshe)

## Что такое ООП? Простыми словами ^chto-takoe-oop-prosto
Представьте, что вы строите дом из кубиков Лего. Каждый кубик - это отдельный объект. ООП - это как раз такой подход к программированию, где мы создаем программу из "кубиков" (объектов), а не пишем одну большую стену кода.

**Простая аналогия:**
- **Класс** - это чертеж кубика Лего (описание, каким он должен быть)
- **Объект** - это конкретный кубик, созданный по чертежу
- **Метод** - это то, что кубик умеет делать (например, соединяться с другими)
- **Поле/свойство** - это характеристики кубика (цвет, размер)

```java
// Это КЛАСС - чертеж для создания объектов
class Student {
    // Поля - характеристики студента
    String name;
    int age;
    String group;
    
    // Методы - что студент умеет делать
    void study() {
        System.out.println(name + " учится");
    }
    
    void sleep() {
        System.out.println(name + " спит");
    }
}
```

<small>Класс - это просто описание, как шаблон. Сам по себе он ничего не делает, пока мы не создадим объект.</small>

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Зачем нужно ООП? ^zachem-nujno-oop
**Без ООП** код выглядит как беспорядочная куча инструкций:
```java
// ПЛОХО: код без ООП - все в одной куче
String student1Name = "Анна";
int student1Age = 20;
String student2Name = "Иван"; 
int student2Age = 21;

// Каждый раз приходится повторять одни и те же действия
System.out.println(student1Name + " учится");
System.out.println(student2Name + " учится");
```

**С ООП** код становится организованным:
```java
// ХОРОШО: код с ООП - все структурировано
class Student {
    String name;
    int age;
    
    void study() {
        System.out.println(name + " учится");
    }
}

// Использование
Student anna = new Student();
anna.name = "Анна";
anna.age = 20;

Student ivan = new Student();
ivan.name = "Иван"; 
ivan.age = 21;

anna.study(); // Анна учится
ivan.study(); // Иван учится
```

**Преимущества ООП:**
- 🎯 **Понятность** - код читается как история
- 🔧 **Переиспользование** - один раз написал, много раз используешь
- 🛠 **Легкость изменений** - меняешь в одном месте, работает везде
- 👥 **Работа в команде** - каждый работает над своими "кубиками"

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Четыре кита ООП ^chetyre-kita-oop
ООП стоит на четырех основных принципах. Давайте разберем каждый на простых примерах.

### Инкапсуляция - "Сейф с данными" ^inkapsulyaciya-dlya-novichkov
**Представьте:** У вас есть банковская карта. Вы знаете, как ей платить, но не знаете, как внутри работает банкомат. Это и есть инкапсуляция - скрытие сложной внутренней работы.

**Что это значит в программировании:**
- Прячем данные внутри объекта
- Даем доступ только через специальные методы
- Защищаем данные от неправильного использования

```java
class BankAccount {
    // private - значит, эти поля доступны ТОЛЬКО внутри этого класса
    private String ownerName;
    private double balance;
    private String password;
    
    // Конструктор - специальный метод для создания объекта
    public BankAccount(String name, String pwd) {
        ownerName = name;
        password = pwd;
        balance = 0.0; // Начальный баланс 0
    }
    
    // PUBLIC методы - это наш "интерфейс" для работы с объектом
    
    // Пополнение счета
    public void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
            System.out.println("Счет пополнен на " + amount + " руб.");
        } else {
            System.out.println("Нельзя пополнить на отрицательную сумму!");
        }
    }
    
    // Снятие денег (с проверкой пароля)
    public void withdraw(double amount, String inputPassword) {
        if (!password.equals(inputPassword)) {
            System.out.println("Неверный пароль!");
            return;
        }
        
        if (amount > 0 && amount <= balance) {
            balance -= amount;
            System.out.println("Снято " + amount + " руб.");
        } else {
            System.out.println("Недостаточно средств или неверная сумма!");
        }
    }
    
    // Просмотр баланса (только если знаем пароль)
    public void checkBalance(String inputPassword) {
        if (password.equals(inputPassword)) {
            System.out.println("Баланс: " + balance + " руб.");
        } else {
            System.out.println("Неверный пароль!");
        }
    }
}

// Использование нашего "сейфа"
public class BankExample {
    public static void main(String[] args) {
        // Создаем банковский счет
        BankAccount myAccount = new BankAccount("Алексей", "1234");
        
        // Работаем ТОЛЬКО через разрешенные методы
        myAccount.deposit(1000);        // Пополняем на 1000 руб.
        myAccount.checkBalance("1234"); // Смотрим баланс (правильный пароль)
        myAccount.checkBalance("0000"); // Пытаемся посмотреть (неправильный пароль)
        myAccount.withdraw(500, "1234"); // Снимаем 500 руб.
        
        // А вот так НЕЛЬЗЯ:
        // myAccount.balance = 1000000; // ОШИБКА: поле private!
        // myAccount.password = "hack"; // ОШИБКА: поле private!
    }
}
```

<small>Инкапсуляция защищает наши данные. Мы не можем напрямую изменить баланс или пароль - только через проверенные методы.</small>

[↑ К содержанию](#^soderganie-dlya-novichkov)

### Наследование - "Семейное сходство" ^nasledovanie-dlya-novichkov
**Представьте:** У вас есть общее понятие "Транспорт". У всех транспорта есть колеса, двигатель, они могут ехать. Машина, мотоцикл, велосипед - все они "наследуют" эти общие свойства от "Транспорта", но у каждого есть что-то свое.

```java
// Родительский класс (общие свойства для ВСЕХ животных)
class Animal {
    String name;
    int age;
    
    // Конструктор
    public Animal(String animalName, int animalAge) {
        name = animalName;
        age = animalAge;
    }
    
    // Общие методы для всех животных
    public void eat() {
        System.out.println(name + " кушает");
    }
    
    public void sleep() {
        System.out.println(name + " спит");
    }
    
    public void makeSound() {
        System.out.println(name + " издает звук");
    }
}

// Дочерний класс - наследует от Animal
class Dog extends Animal {
    String breed; // НОВОЕ свойство, которого нет у Animal
    
    // Конструктор
    public Dog(String dogName, int dogAge, String dogBreed) {
        super(dogName, dogAge); // super - вызов конструктора родителя
        breed = dogBreed;
    }
    
    // НОВЫЙ метод, которого нет у Animal
    public void fetch() {
        System.out.println(name + " приносит палку");
    }
    
    // ПЕРЕОПРЕДЕЛЕНИЕ метода родителя
    @Override
    public void makeSound() {
        System.out.println(name + " лает: Гав-гав!");
    }
}

// Еще один дочерний класс
class Cat extends Animal {
    int livesLeft = 9;
    
    public Cat(String catName, int catAge) {
        super(catName, catAge);
    }
    
    @Override
    public void makeSound() {
        System.out.println(name + " мяукает: Мяу-мяу!");
    }
    
    public void climbTree() {
        System.out.println(name + " лазает по деревьям");
    }
}

// Используем наше "семейство" животных
public class InheritanceExample {
    public static void main(String[] args) {
        // Создаем животных
        Dog myDog = new Dog("Бобик", 3, "Овчарка");
        Cat myCat = new Cat("Мурка", 2);
        
        // Общие методы (унаследованные от Animal)
        myDog.eat();        // Бобик кушает
        myDog.sleep();      // Бобик спит
        myCat.eat();        // Мурка кушает
        
        // Уникальные методы
        myDog.fetch();      // Бобик приносит палку
        myCat.climbTree();  // Мурка лазает по деревьям
        
        // Переопределенные методы
        myDog.makeSound();  // Бобик лает: Гав-гав!
        myCat.makeSound();  // Мурка мяукает: Мяу-мяу!
        
        // Демонстрация наследования
        System.out.println("Собака: " + myDog.name + ", порода: " + myDog.breed);
        System.out.println("Кот: " + myCat.name + ", осталось жизней: " + myCat.livesLeft);
    }
}
```

**Что дает наследование:**
- ♻️ **Переиспользование кода** - не нужно повторять общие свойства
- 🎯 **Логическая структура** - понятные отношения между классами
- 🔧 **Легкость расширения** - легко добавлять новые виды животных

[↑ К содержанию](#^soderganie-dlya-novichkov)

### Полиморфизм - "Одно имя, разные действия" ^polimorfizm-dlya-novichkov
**Представьте:** Слово "открыть". Вы можете "открыть" дверь, "открыть" книгу, "открыть" программу. Действие одно, но выполняется по-разному. Это и есть полиморфизм!

```java
// Базовый класс "Фигура"
class Shape {
    String color;
    
    public Shape(String shapeColor) {
        color = shapeColor;
    }
    
    // Метод, который будет по-разному работать для разных фигур
    public double calculateArea() {
        return 0; // Базовая реализация
    }
    
    public void draw() {
        System.out.println("Рисую фигуру цвета " + color);
    }
}

// Круг - конкретная фигура
class Circle extends Shape {
    double radius;
    
    public Circle(String color, double circleRadius) {
        super(color);
        radius = circleRadius;
    }
    
    // ПЕРЕОПРЕДЕЛЯЕМ метод calculateArea для круга
    @Override
    public double calculateArea() {
        return 3.14159 * radius * radius; // Площадь круга: πr²
    }
    
    @Override
    public void draw() {
        System.out.println("Рисую круг радиусом " + radius + " цвета " + color);
    }
}

// Прямоугольник - другая фигура
class Rectangle extends Shape {
    double width;
    double height;
    
    public Rectangle(String color, double rectWidth, double rectHeight) {
        super(color);
        width = rectWidth;
        height = rectHeight;
    }
    
    // ПЕРЕОПРЕДЕЛЯЕМ метод calculateArea для прямоугольника
    @Override
    public double calculateArea() {
        return width * height; // Площадь прямоугольника: ширина × высота
    }
    
    @Override
    public void draw() {
        System.out.println("Рисую прямоугольник " + width + "x" + height + " цвета " + color);
    }
}

// Демонстрация полиморфизма
public class PolymorphismExample {
    public static void main(String[] args) {
        // Создаем массив РАЗНЫХ фигур, но одного типа Shape
        Shape[] shapes = new Shape[3];
        
        shapes[0] = new Shape("красный");           // Просто фигура
        shapes[1] = new Circle("синий", 5.0);       // Круг
        shapes[2] = new Rectangle("зеленый", 4.0, 6.0); // Прямоугольник
        
        // ВОТ ОН - ПОЛИМОРФИЗМ!
        // Один и тот же код работает с РАЗНЫМИ типами фигур
        for (int i = 0; i < shapes.length; i++) {
            shapes[i].draw();
            double area = shapes[i].calculateArea();
            System.out.println("Площадь: " + area);
            System.out.println("---");
        }
        
        // Еще один пример полиморфизма
        System.out.println("=== Разные фигуры, один метод ===");
        printShapeInfo(new Circle("желтый", 3.0));
        printShapeInfo(new Rectangle("фиолетовый", 2.0, 8.0));
    }
    
    // Этот метод может работать с ЛЮБОЙ фигурой!
    public static void printShapeInfo(Shape shape) {
        shape.draw();
        System.out.println("Площадь: " + shape.calculateArea());
        System.out.println();
    }
}
```

**Что происходит в этом примере:**
- У нас есть метод `calculateArea()` 
- Для круга он считает площадь по формуле πr²
- Для прямоугольника - по формуле ширина × высота
- Для базовой фигуры - возвращает 0

**Преимущества полиморфизма:**
- 🎯 **Гибкость** - один код работает со многими типами
- 🔧 **Расширяемость** - легко добавить новую фигуру
- 📝 **Читаемость** - код понятен и логичен

[↑ К содержанию](#^soderganie-dlya-novichkov)

### Абстракция - "Упрощение сложного" ^abstrakciya-dlya-novichkov
**Представьте:** Вы пользуетесь автомобилем. Вам не нужно знать, как работает двигатель, коробка передач, система впрыска топлива. Вы просто нажимаете педаль газа и поворачиваете руль. Это абстракция - скрытие сложной реализации и показ только важных деталей.

```java
// Абстрактный класс - нельзя создать объект этого класса
// Это просто "идея" транспортного средства
abstract class Vehicle {
    protected String brand;
    protected int speed;
    
    public Vehicle(String vehicleBrand, int vehicleSpeed) {
        brand = vehicleBrand;
        speed = vehicleSpeed;
    }
    
    // Абстрактный метод - нет реализации, только описание ЧТО делать
    // Каждый наследник ДОЛЖЕН реализовать этот метод по-своему
    public abstract void move();
    
    // Обычный метод - есть реализация
    public void displayInfo() {
        System.out.println("Марка: " + brand + ", Макс. скорость: " + speed + " км/ч");
    }
}

// Конкретная реализация - автомобиль
class Car extends Vehicle {
    int doorsCount;
    
    public Car(String brand, int speed, int doors) {
        super(brand, speed);
        doorsCount = doors;
    }
    
    // РЕАЛИЗУЕМ абстрактный метод
    @Override
    public void move() {
        System.out.println(brand + " едет по дороге на " + doorsCount + " дверях");
    }
}

// Другая реализация - самолет
class Airplane extends Vehicle {
    int wingspan;
    
    public Airplane(String brand, int speed, int span) {
        super(brand, speed);
        wingspan = span;
    }
    
    // РЕАЛИЗУЕМ абстрактный метод по-своему
    @Override
    public void move() {
        System.out.println(brand + " летит в небе с размахом крыльев " + wingspan + " м");
    }
}

// Интерфейс - это контракт "что умеет делать"
// Класс, реализующий интерфейс, ОБЯЗАН реализовать все его методы
interface Electric {
    void charge();          // Метод для зарядки
    int getBatteryLevel();  // Метод для получения уровня заряда
}

// Электромобиль - и транспорт, и электрический
class ElectricCar extends Vehicle implements Electric {
    private int batteryLevel;
    
    public ElectricCar(String brand, int speed, int battery) {
        super(brand, speed);
        batteryLevel = battery;
    }
    
    @Override
    public void move() {
        if (batteryLevel > 10) {
            System.out.println(brand + " бесшумно едет на электричестве");
            batteryLevel -= 5;
        } else {
            System.out.println(brand + ": Низкий заряд батареи! Нужно зарядить!");
        }
    }
    
    // Реализация методов интерфейса Electric
    @Override
    public void charge() {
        batteryLevel = 100;
        System.out.println(brand + " заряжен до 100%");
    }
    
    @Override
    public int getBatteryLevel() {
        return batteryLevel;
    }
}

// Использование абстракции
public class AbstractionExample {
    public static void main(String[] args) {
        // Можно создавать объекты конкретных классов
        Car myCar = new Car("Toyota", 180, 4);
        Airplane myPlane = new Airplane("Boeing", 900, 35);
        ElectricCar myTesla = new ElectricCar("Tesla", 250, 50);
        
        // Работаем через абстракцию
        Vehicle[] vehicles = {myCar, myPlane, myTesla};
        
        for (Vehicle vehicle : vehicles) {
            vehicle.displayInfo();
            vehicle.move(); // Каждый двигается по-своему!
            System.out.println();
        }
        
        // Работа с электрическим транспортом
        System.out.println("Уровень заряда Tesla: " + myTesla.getBatteryLevel() + "%");
        myTesla.charge();
        System.out.println("После зарядки: " + myTesla.getBatteryLevel() + "%");
        
        // А вот так НЕЛЬЗЯ:
        // Vehicle v = new Vehicle("Test", 100); // ОШИБКА: нельзя создать объект абстрактного класса
    }
}
```

**Что дает абстракция:**
- 🛡 **Защита от ошибок** - нельзя создать "просто транспорт", только конкретный
- 📝 **Четкие договоренности** - интерфейсы говорят "что делать", а классы решают "как делать"
- 🔧 **Снижение сложности** - работаем только с важными деталями

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Первый класс - создаем свой объект ^pervyj-klass-prosto
Давайте создадим свой первый полезный класс шаг за шагом!

```java
// Шаг 1: Придумываем, что мы хотим создать
// Давайте создадим класс "Книга"

class Book {
    // Шаг 2: Определяем характеристики (поля)
    String title;       // Название
    String author;      // Автор
    int year;           // Год издания
    int pages;          // Количество страниц
    boolean isRead;     // Прочитана ли книга
    
    // Шаг 3: Создаем конструктор - специальный метод для создания объекта
    public Book(String bookTitle, String bookAuthor, int bookYear, int bookPages) {
        title = bookTitle;
        author = bookAuthor;
        year = bookYear;
        pages = bookPages;
        isRead = false; // По умолчанию книга не прочитана
    }
    
    // Шаг 4: Добавляем методы - что книга умеет "делать"
    
    // Метод для чтения книги
    public void read() {
        if (!isRead) {
            System.out.println("Вы начали читать: " + title);
            isRead = true;
        } else {
            System.out.println("Вы уже читали эту книгу: " + title);
        }
    }
    
    // Метод для отображения информации о книге
    public void displayInfo() {
        System.out.println("'" + title + "'");
        System.out.println("Автор: " + author);
        System.out.println("Год: " + year + ", Страниц: " + pages);
        System.out.println("Прочитана: " + (isRead ? "Да" : "Нет"));
        System.out.println();
    }
    
    // Метод для получения возраста книги
    public int getAge(int currentYear) {
        return currentYear - year;
    }
}

// Шаг 5: Используем наш класс!
public class MyFirstClass {
    public static void main(String[] args) {
        // Создаем объекты книг
        Book book1 = new Book("Война и мир", "Лев Толстой", 1869, 1225);
        Book book2 = new Book("Мастер и Маргарита", "Михаил Булгаков", 1967, 480);
        
        // Работаем с книгами
        System.out.println("=== Мои книги ===");
        book1.displayInfo();
        book2.displayInfo();
        
        // Читаем книги
        book1.read();
        book2.read();
        
        // Пытаемся прочитать еще раз
        book1.read();
        
        // Смотрим возраст книг
        System.out.println("Возраст 'Войны и мира': " + book1.getAge(2024) + " лет");
        System.out.println("Возраст 'Мастера и Маргариты': " + book2.getAge(2024) + " лет");
    }
}
```

**Что мы сделали:**
1. ✅ Создали класс `Book` с полями (характеристики)
2. ✅ Добавили конструктор для создания объектов
3. ✅ Добавили методы (действия)
4. ✅ Создали и использовали объекты

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Практические примеры из жизни ^primery-iz-zhizni
Давайте создадим полезную программу для учета студентов!

```java
import java.util.ArrayList;
import java.util.List;

class Student {
    // Поля с инкапсуляцией (private)
    private String name;
    private int age;
    private String group;
    private List<Integer> grades; // Список оценок
    
    // Конструктор
    public Student(String studentName, int studentAge, String studentGroup) {
        name = studentName;
        age = studentAge;
        group = studentGroup;
        grades = new ArrayList<>();
    }
    
    // Методы для работы с оценками
    public void addGrade(int grade) {
        if (grade >= 1 && grade <= 5) {
            grades.add(grade);
            System.out.println(name + " получил(а) оценку: " + grade);
        } else {
            System.out.println("Ошибка: оценка должна быть от 1 до 5");
        }
    }
    
    // Расчет среднего балла
    public double getAverageGrade() {
        if (grades.isEmpty()) {
            return 0.0;
        }
        
        int sum = 0;
        for (int grade : grades) {
            sum += grade;
        }
        return (double) sum / grades.size();
    }
    
    // Показать информацию о студенте
    public void displayInfo() {
        System.out.println("Студент: " + name);
        System.out.println("Возраст: " + age + ", Группа: " + group);
        System.out.println("Оценки: " + grades);
        System.out.println("Средний балл: " + String.format("%.2f", getAverageGrade()));
        System.out.println();
    }
    
    // Геттеры (методы для получения значений)
    public String getName() { return name; }
    public int getAge() { return age; }
    public String getGroup() { return group; }
}

// Класс для управления группой студентов
class StudentGroup {
    private String groupName;
    private List<Student> students;
    
    public StudentGroup(String name) {
        groupName = name;
        students = new ArrayList<>();
    }
    
    // Добавить студента в группу
    public void addStudent(Student student) {
        students.add(student);
        System.out.println(student.getName() + " добавлен(а) в группу " + groupName);
    }
    
    // Показать всех студентов группы
    public void displayAllStudents() {
        System.out.println("=== Группа: " + groupName + " ===");
        for (Student student : students) {
            student.displayInfo();
        }
    }
    
    // Найти студента по имени
    public Student findStudent(String name) {
        for (Student student : students) {
            if (student.getName().equalsIgnoreCase(name)) {
                return student;
            }
        }
        return null; // Не нашли
    }
    
    // Расчет среднего балла всей группы
    public double getGroupAverage() {
        if (students.isEmpty()) {
            return 0.0;
        }
        
        double total = 0.0;
        for (Student student : students) {
            total += student.getAverageGrade();
        }
        return total / students.size();
    }
}

// Главная программа
public class UniversitySystem {
    public static void main(String[] args) {
        // Создаем группу
        StudentGroup group = new StudentGroup("ИТ-21");
        
        // Создаем студентов
        Student anna = new Student("Анна", 20, "ИТ-21");
        Student ivan = new Student("Иван", 21, "ИТ-21");
        Student maria = new Student("Мария", 19, "ИТ-21");
        
        // Добавляем студентов в группу
        group.addStudent(anna);
        group.addStudent(ivan);
        group.addStudent(maria);
        
        // Добавляем оценки
        System.out.println("\n=== Добавление оценок ===");
        anna.addGrade(5);
        anna.addGrade(4);
        anna.addGrade(5);
        
        ivan.addGrade(3);
        ivan.addGrade(4);
        ivan.addGrade(4);
        
        maria.addGrade(5);
        maria.addGrade(5);
        maria.addGrade(5);
        
        // Показываем информацию
        System.out.println("\n=== Информация о группе ===");
        group.displayAllStudents();
        
        // Средний балл группы
        System.out.println("Средний балл группы: " + String.format("%.2f", group.getGroupAverage()));
        
        // Поиск студента
        System.out.println("\n=== Поиск студента ===");
        Student found = group.findStudent("Анна");
        if (found != null) {
            System.out.println("Найден студент:");
            found.displayInfo();
        }
    }
}
```

**Что мы создали:**
- ✅ Систему учета студентов
- ✅ Возможность добавлять оценки
- ✅ Расчет средних баллов
- ✅ Поиск студентов
- ✅ Все принципы ООП в одном примере!

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Частые ошибки новичков ^oshibki-novichkov

### Ошибка 1: Непонимание разницы между классом и объектом
```java
// НЕПРАВИЛЬНО
class Dog {
    String name;
}

Dog.name = "Бобик"; // ОШИБКА! У класса нет имени, у объекта есть

// ПРАВИЛЬНО
class Dog {
    String name;
}

Dog myDog = new Dog(); // Создаем ОБЪЕКТ
myDog.name = "Бобик";  // Работаем с объектом
```

### Ошибка 2: Попытка использовать объект без создания
```java
// НЕПРАВИЛЬНО
Student student;
student.name = "Анна"; // ОШИБКА! Объект не создан

// ПРАВИЛЬНО
Student student = new Student("Анна", 20, "ИТ-21");
```

### Ошибка 3: Неиспользование инкапсуляции
```java
// НЕПРАВИЛЬНО (данные не защищены)
class BankAccount {
    public double balance; // ОПАСНО! Можно изменить напрямую
}

BankAccount account = new BankAccount();
account.balance = -1000000; // Украли деньги!

// ПРАВИЛЬНО
class BankAccount {
    private double balance;
    
    public void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
        }
    }
}
```

### Ошибка 4: Путаница с static
```java
class Calculator {
    // static - принадлежит КЛАССУ, а не объекту
    static int add(int a, int b) {
        return a + b;
    }
}

// НЕПРАВИЛЬНО
Calculator calc = new Calculator();
int result = calc.add(5, 3); // Работает, но нелогично

// ПРАВИЛЬНО
int result = Calculator.add(5, 3); // Вызов через класс
```

[↑ К содержанию](#^soderganie-dlya-novichkov)

## Что учить дальше? ^chto-uchit-dalshe
После освоения основ ООП рекомендуем изучить:

### 1. Конструкторы и перегрузка методов
```java
class Person {
    String name;
    int age;
    
    // Конструктор по умолчанию
    public Person() {
        name = "Неизвестно";
        age = 0;
    }
    
    // Конструктор с параметрами
    public Person(String personName) {
        name = personName;
        age = 0;
    }
    
    // Конструктор со всеми параметрами
    public Person(String personName, int personAge) {
        name = personName;
        age = personAge;
    }
}
```

### 2. Коллекции и работа с данными
```java
import java.util.*;

List<Student> students = new ArrayList<>();
Map<String, Student> studentMap = new HashMap<>();
```

### 3. Исключения (обработка ошибок)
```java
try {
    // Код, который может вызвать ошибку
    int result = 10 / 0;
} catch (ArithmeticException e) {
    System.out.println("Ошибка: деление на ноль!");
}
```

### 4. Потоки ввода-вывода
```java
// Чтение из файла, работа с сетью и т.д.
```

### План обучения:
1. **Неделя 1-2:** Практика с классами и объектами
2. **Неделя 3-4:** Глубокое понимание наследования и полиморфизма  
3. **Неделя 5-6:** Изучение стандартных библиотек Java
4. **Неделя 7-8:** Создание своего первого полноценного проекта

**Совет:** Лучший способ научиться - практика! Создавайте маленькие проекты:
- Система учета книг в библиотеке
- Простой банковский счет
- Игра "Угадай число"
- База данных сотрудников

[↑ К содержанию](#^soderganie-dlya-novichkov)

**Теги:** #java-для-начинающих #ооп-основы #программирование #java #обучение-программированию #инкапсуляция #наследование #полиморфизм #абстракция
```